import { RouteConfig } from '../types';

/**
 * Domain-specific module routes.
 * Add your project's custom module routes here.
 */
const moduleRoutes: RouteConfig[] = [];

export default moduleRoutes;
